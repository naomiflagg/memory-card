import React from 'react';

const Header = () => {
  return (
    <header>
      This is a game of memory. Click on as many unique images as you can. If
      you click on an image that you have already clicked on, the game is over, and your score resets.
    </header>
  );
};

export default Header;
